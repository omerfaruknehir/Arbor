name: Verify Arbor 0.22.3 scroll physics

on:
  push:
    branches:
      - release/scroll-physics-0.22.3
  workflow_dispatch:

permissions:
  contents: write

jobs:
  integrate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: release/scroll-physics-0.22.3
          fetch-depth: 0

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Set up Android 16 SDK
        uses: android-actions/setup-android@v4
        with:
          packages: 'platform-tools platforms;android-36 build-tools;36.0.0'

      - uses: gradle/actions/setup-gradle@v4
        with:
          cache-read-only: true

      - name: Apply integrated release patch
        run: python3 tools/apply-scroll-physics-0223.py

      - name: Run release unit tests
        run: ./gradlew --no-daemon --stacktrace --max-workers=2 -Pkotlin.compiler.execution.strategy=in-process testReleaseUnitTest

      - name: Run release lint
        run: ./gradlew --no-daemon --stacktrace --max-workers=2 -Pkotlin.compiler.execution.strategy=in-process lintRelease

      - name: Assemble optimized release APK
        run: ./gradlew --no-daemon --stacktrace --max-workers=2 -Pkotlin.compiler.execution.strategy=in-process assembleRelease

      - name: Commit verified release changes
        shell: bash
        run: |
          git rm .github/workflows/agent-scroll-physics-0223.yml tools/apply-scroll-physics-0223.py
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add app docs/releases/RELEASE_NOTES_0.22.3.md
          git commit -m 'Fix app-bar scroll physics in Arbor 0.22.3'
          git push origin HEAD:release/scroll-physics-0.22.3
