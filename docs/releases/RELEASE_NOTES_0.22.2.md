# Arbor 0.22.2

- Let the Android IME own predictive Back while the keyboard is visible.
- Prevent drawer and page navigation from consuming the same Back gesture as the keyboard.
- Replace the oversized collapsing Search header with a compact search-first layout.
- Keep the search field pinned above results and respect IME insets.
- Add clear, empty-query, and no-results states.
